# python

- Creates a symlink for `$HOME/.pythonstartup` to point to the file in here
- Uses [gnureadline](https://pypi.org/project/gnureadline/) to provide readline support for the system Python distribution (in particular for history/completion in the interactive interpreter) on macOS
